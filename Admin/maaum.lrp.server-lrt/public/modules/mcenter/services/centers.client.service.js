'use strict';

//Centers service used to communicate Centers REST endpoints
// users안에서 Centers factory를 이미 선언함
/*angular.module('centers').factory('Centers', ['$resource',
	function($resource) {
		return $resource('centers/:centerId', { centerId: '@_id'
		}, {
			update: {
				method: 'PUT'
			}
		});
	}
]);*/
